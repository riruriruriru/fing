<%= form_with(model: student_tutorial, local: true) do |form| %>
  <% if student_tutorial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_tutorial.errors.count, "error") %> prohibited this student_tutorial from being saved:</h2>

      <ul>
      <% student_tutorial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
